---
import AdminLayout from '../layouts/AdminLayout.astro';
import StatCard from '../components/StatCard.astro';

const portafolio = {
	disponibles: 8,
	total: 10,
};

const stats = [
	{ value: 1, label: 'en negociación' },
	{ value: 1, label: 'cerrados' },
	{ value: 10, label: 'en portafolio' },
];

const porCiudad = [
	{ ciudad: 'Bogotá', count: 1 },
	{ ciudad: 'Medellín', count: 4 },
	{ ciudad: 'Cartagena', count: 11 },
	{ ciudad: 'Armenia', count: 11 },
];

const porTipo = [
	{ tipo: 'Apartamento', count: 4 },
	{ tipo: 'Casa', count: 11 },
	{ tipo: 'Oficina', count: 8 },
	{ tipo: 'Finca campestre', count: 6 },
];

const recientes = [
	{
		title: 'Penthouse Panorámico en El Rosales',
		photo: 'https://images.unsplash.com/photo-1600596542815-ffad4c1539a9?w=120&q=80',
	},
	{
		title: 'Apartamento Moderno en El Poblado',
		photo: 'https://images.unsplash.com/photo-1512917774080-9991f1c4c750?w=120&q=80',
	},
	{
		title: 'Casa Colonial en Castillogrande',
		photo: 'https://images.unsplash.com/photo-1580587771525-78b9dba3b914?w=120&q=80',
	},
	{
		title: 'La castellana',
		photo: 'https://images.unsplash.com/photo-1564013799919-ab600027ffc6?w=120&q=80',
	},
];
---

<AdminLayout title="Dashboard - Sansalica">
	<div class="dashboard">

		<header class="dash-header">
			<span class="dash-subtitle">PANEL GENERAL</span>
			<h1 class="dash-title">Dashboard</h1>
		</header>

		<div class="portafolio-card">
			<div class="portafolio-body">
				<p class="portafolio-label">PORTAFOLIO ACTIVO</p>
				<p class="portafolio-number">{portafolio.disponibles}</p>
				<p class="portafolio-desc">
					inmuebles disponibles de {portafolio.total} totales
				</p>
			</div>
			<div class="portafolio-circle" aria-hidden="true"></div>
		</div>

		<div class="stats-grid">
			<StatCard value={stats[0].value} label={stats[0].label}>
				<svg slot="icon" xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
					<circle cx="12" cy="12" r="10"></circle>
					<polyline points="12 6 12 12 16 14"></polyline>
				</svg>
			</StatCard>

			<StatCard value={stats[1].value} label={stats[1].label}>
				<svg slot="icon" xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
					<polyline points="22 7 13.5 15.5 8.5 10.5 2 17"></polyline>
					<polyline points="16 7 22 7 22 13"></polyline>
				</svg>
			</StatCard>

			<StatCard value={stats[2].value} label={stats[2].label}>
				<svg slot="icon" xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
					<line x1="8" y1="6" x2="21" y2="6"></line>
					<line x1="8" y1="12" x2="21" y2="12"></line>
					<line x1="8" y1="18" x2="21" y2="18"></line>
					<line x1="3" y1="6" x2="3.01" y2="6"></line>
					<line x1="3" y1="12" x2="3.01" y2="12"></line>
					<line x1="3" y1="18" x2="3.01" y2="18"></line>
				</svg>
			</StatCard>
		</div>

		<div class="info-grid">

			<div class="info-card">
				<p class="info-card-title">POR CIUDAD</p>
				<ul class="info-list">
					{porCiudad.map(({ ciudad, count }) => (
						<li class="info-row">
							<span class="info-row-name">{ciudad}</span>
							<span class="info-row-divider" aria-hidden="true"></span>
							<span class="info-row-count">{count}</span>
						</li>
					))}
				</ul>
			</div>

			<div class="info-card">
				<p class="info-card-title">POR TIPO</p>
				<ul class="info-list">
					{porTipo.map(({ tipo, count }) => (
						<li class="info-row">
							<span class="info-row-name">{tipo}</span>
							<span class="info-pill">{count}</span>
						</li>
					))}
				</ul>
			</div>

			<div class="info-card">
				<p class="info-card-title">RECIENTES</p>
				<ul class="recientes-list">
					{recientes.map(({ title, photo }) => (
						<li class="reciente-item">
							<img src={photo} alt={title} class="reciente-img" />
							<p class="reciente-title">{title}</p>
						</li>
					))}
				</ul>
			</div>

		</div>
	</div>
</AdminLayout>

<style>
	.dashboard {
		display: flex;
		flex-direction: column;
		gap: 24px;
		width: 100%;
		max-width: 1000px;
	}

	.dash-header {
		display: flex;
		flex-direction: column;
		gap: 2px;
	}

	.dash-subtitle {
		font-family: var(--font-primary);
		font-size: 13px;
		font-weight: 600;
		letter-spacing: 2px;
		color: var(--color-gold);
		text-transform: uppercase;
	}

	.dash-title {
		font-family: var(--font-primary);
		font-size: 42px;
		font-weight: 700;
		color: var(--color-navy);
		margin: 0;
		line-height: 1.1;
	}

	.portafolio-card {
		position: relative;
		background-color: rgba(207, 172, 109, 0.36);
		border-radius: 16px;
		padding: 36px 40px;
		overflow: hidden;
		display: flex;
		align-items: center;
	}

	.portafolio-body {
		display: flex;
		flex-direction: column;
		gap: 6px;
		z-index: 1;
	}

	.portafolio-label {
		font-family: var(--font-primary);
		font-size: 13px;
		font-weight: 600;
		letter-spacing: 2px;
		color: var(--color-navy);
		margin: 0;
		text-transform: uppercase;
	}

	.portafolio-number {
		font-family: var(--font-primary);
		font-size: 72px;
		font-weight: 700;
		color: var(--color-navy);
		margin: 0;
		line-height: 1;
	}

	.portafolio-desc {
		font-family: var(--font-primary);
		font-size: 16px;
		color: var(--color-navy);
		margin: 0;
		opacity: 0.8;
	}

	.portafolio-circle {
		position: absolute;
		top: -50px;
		right: -50px;
		width: 200px;
		height: 200px;
		border-radius: 50%;
		background-color: var(--color-gold);
		opacity: 0.5;
	}

	.stats-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 20px;
	}

	.info-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 20px;
		align-items: start;
	}

	.info-card {
		background-color: var(--color-white);
		border-radius: 16px;
		padding: 24px;
		box-shadow: 0 2px 12px rgba(0, 0, 0, 0.04);
		display: flex;
		flex-direction: column;
		gap: 16px;
	}

	.info-card-title {
		font-family: var(--font-primary);
		font-size: 11px;
		font-weight: 700;
		letter-spacing: 2px;
		color: var(--color-navy);
		margin: 0;
		text-transform: uppercase;
	}

	.info-list {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 14px;
	}

	.info-row {
		display: flex;
		align-items: center;
		gap: 8px;
	}

	.info-row-name {
		font-family: var(--font-primary);
		font-size: 13px;
		color: var(--color-navy);
		white-space: nowrap;
	}

	.info-row-divider {
		flex: 1;
		height: 1px;
		background-color: var(--color-gold-light);
		opacity: 0.5;
	}

	.info-row-count {
		font-family: var(--font-primary);
		font-size: 13px;
		color: var(--color-navy);
		font-weight: 600;
	}

	.info-pill {
		margin-left: auto;
		background-color: rgba(207, 172, 109, 0.2);
		color: var(--color-gold);
		font-size: 11px;
		font-weight: 700;
		padding: 3px 10px;
		border-radius: 999px;
		min-width: 28px;
		text-align: center;
	}

	.recientes-list {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 12px;
	}

	.reciente-item {
		display: flex;
		align-items: center;
		gap: 12px;
	}

	.reciente-img {
		width: 52px;
		height: 40px;
		object-fit: cover;
		border-radius: 8px;
		flex-shrink: 0;
	}

	.reciente-title {
		font-family: var(--font-primary);
		font-size: 12px;
		color: var(--color-navy);
		margin: 0;
		line-height: 1.4;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}

	@media (max-width: 900px) {
		.stats-grid {
			grid-template-columns: repeat(2, 1fr);
		}

		.info-grid {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 560px) {
		.stats-grid {
			grid-template-columns: 1fr 1fr;
		}

		.dash-title {
			font-size: 28px;
		}

		.portafolio-number {
			font-size: 48px;
		}

		.portafolio-card {
			padding: 24px 20px;
		}

		.info-grid {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 380px) {
		.stats-grid {
			grid-template-columns: 1fr;
		}

		.dash-title {
			font-size: 22px;
		}
	}
</style>
